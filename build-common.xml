<?xml version="1.0"?>
<project name="common-libs-common" xmlns:ivy="antlib:org.apache.ivy.ant">

	<property name="project.revision" value="1.0.1"/>

	<property name="debug.mode" value="true"/>
	<property name="ant.build.javac.target" value="1.6"/>
	<property name="ant.build.javac.source" value="1.6"/>
	
	<property name="bundle.dest" value=".."/>

	<target name="prepare-jar-name">
		<!-- the name of the plug-in jar is the project name of the calling build file with '.jar' appended -->
		<property name="jar.name" value="${bundle.dest}/${ant.project.name}.jar"/>
	</target>

	<target name="prepare-common" depends="prepare-jar-name">
		<mkdir dir="build"/>
		<tstamp>
			<format property="builddate" pattern="yyyyMMdd"/>
		</tstamp>
		<exec executable="git" outputproperty="githash" failifexecutionfails="false">
			<arg line="log -1 --format='%H'"/>
		</exec>
	</target>
	
	<target name="prepare">
	</target>
	
	<target name="clean">
		<delete dir="build"/>
	</target>

	<target name="dist-clean" depends="clean">
		<delete file="${bundle.dest}/${jar.name}"/>
	</target>

	<target name="retrieve">
		<ivy:retrieve/>
	</target>
	
	<target name="compile" depends="prepare,prepare-common,retrieve">
		<echo>Compiling: ${jar.name}</echo>
		<javac srcdir="src"
			   includes="**"
			   destdir="build"
			   debug="${debug.mode}"
			   source="1.6">
		  <classpath refid="project.class.path"/>
		</javac>
		<!-- workaround: make an empty directory so that the next step doesn't fail -->
		<mkdir dir="resources"/>
		<copy todir="build">
			<fileset dir="resources">
				<include name="**/*"/>
			</fileset>
		</copy>
	</target>

	<target name="jar" depends="compile"> 
		<echo>Creating JAR: ${jar.name}</echo>
		<jar jarfile="${jar.name}" basedir="build" manifest="META-INF/MANIFEST.MF">
			<manifest>
					<attribute name="Built-Date" value="${builddate}"/>
					<attribute name="Built-By" value="${user.name}"/>
					<attribute name="Git-Hash" value="${githash}"/>
			</manifest>
		</jar>
	</target>
	
	<target name="test">
		<javac srcdir="test" debug="${debug.mode}"
					includes="**"
					destdir="build"
					source="1.6">
			<classpath>
				<path refid="test.class.path"/>
				<path refid="project.class.path"/>
			</classpath>
		</javac>
		<junit printsummary="on" haltonfailure="true" fork="true" dir=".">
			<formatter type="brief" usefile="false"/>
			<classpath>
				<pathelement path="build"/>
				<path refid="project.class.path"/>
				<path refid="test.class.path"/>
			</classpath>
			<batchtest>
				<fileset dir="test">
					<include name="**/*Test*.java"/>
				</fileset>
			</batchtest>
		</junit>
	</target>

	<target name="publish" depends="clean,jar" description="Publish this build into repository">
		<echo>Checking if git repository is clean
If exec returns 1, you have to git commit first</echo>
		<exec executable="git" outputproperty="diff" failifexecutionfails="false" failonerror="true">
			<!-- this will cause task to fail if there are dirty commits -->
			<!-- however, if the git executable can't be run, this check is skipped -->
			<arg line="diff --exit-code"/>
		</exec>
		<echo message="Repository OK"/>
		<ivy:resolve/>
		<ivy:publish resolver="libraries" overwrite="true" forcedeliver="true" pubrevision="${project.revision}" status="release">
			<artifacts pattern="${bundle.dest}/[artifact].[ext]"/>
		</ivy:publish>
		<ivy:publish resolver="artifactory_publish" overwrite="true" forcedeliver="true" pubrevision="${project.revision}" status="release">
			<artifacts pattern="${bundle.dest}/[artifact].[ext]"/>
		</ivy:publish>
		<echo message="project ${ant.project.name} released with version ${project.revision}"/>
	</target>

</project>
